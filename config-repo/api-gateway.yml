spring:
  session:
    store-type: redis
    timeout: 30m

  data:
    redis:
      host: bank-redis
      port: 6379

  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: issuer-uri: http://bank-keycloak:8080/realms/bank-realm
        registration:
          gateway:
            provider: keycloak
            client-id: bank-gateway
            client-secret: wzpcA6bISWDI3oTn6eJaXS2eIXIj6wKw
            scope:
              - openid
              - profile
              - email

  cloud:
    gateway:
      default-filters:
        - SaveSession
      routes:
        - id: account-service
          uri: lb://ACCOUNT-SERVICE
          predicates:
            - Path=/accounts/**

eureka:
  client:
    service-url:
      defaultZone: http://bank-eureka-server:8761/eureka
