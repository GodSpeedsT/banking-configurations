server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://AUTH-SERVICE 
          predicates:
            - Path=/auth/**, /userinfo/**
        - id: deposit-service
          uri: lb://DEPOSIT-SERVICE 
          predicates:
            - Path=/api/deposits/**, /api/accounts/**
          filters:
            - TokenRelay
  session:
    store-type: redis
